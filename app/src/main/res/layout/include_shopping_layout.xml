<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_above="@+id/lineBar_layout_container"
    android:scrollbars="none"
    tools:showIn="@layout/gridview_detail_layout">
    <RelativeLayout
        android:id="@+id/shopping_layout_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:visibility="gone">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <include layout="@layout/include_add_to_shopping_layout" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="480dp">

                <ImageView
                    android:id="@+id/image_mainImage_detail"
                    android:layout_width="130dp"
                    android:layout_height="100dp"
                    android:background="@drawable/bg_shape_rectangle"
                    android:scaleType="fitXY"
                    android:src="@mipmap/ic_launcher"
                    android:visibility="gone" />
            </LinearLayout>

        </RelativeLayout>
    </RelativeLayout>

</ScrollView>

